{
  "name": "jlg-volunteer-management",
  "version": "1.0.0",
  "private": true,
  "type": "module",
  "scripts": {
    "build": "tsc -p tsconfig.json",
    "lint": "tsc --noEmit",
    "seed": "node --loader ts-node/esm scripts/seed.ts"
  },
  "dependencies": {
    "firebase": "^11.0.0",
    "zod": "^3.23.8"
  },
  "devDependencies": {
    "ts-node": "^10.9.2",
    "typescript": "^5.6.3"
  }
}
